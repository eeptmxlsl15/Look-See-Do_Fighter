enum PlayerMoveState : Byte {
  Idle,
  WalkForward,
  WalkBackward,
  DashForward,
  DashBackward,
  Crouch
}


component LSDF_Player
{	
	bool isDashBack;
	bool isDashFront;
    bool isSit;
	bool isStandUpGuard;
	bool isAttack;
    bool isHit;
	bool isGuard;
	bool isStun;
	bool isCombo;
	bool canCounter;
	bool isJump;
	int playerHp;
	int DelayFrame;

	array<int>[28] CommandSkillMap;
	
}	